Need to Install:
	Anaconda(python-3)

cmd:
	1) conda install -c conda-forge keras
	2) pip install ftfy
	3) pip install --upgrade gensim
	4) first timw only we need to do download.nlp
conda install -c conda-forge keras==2.2.5